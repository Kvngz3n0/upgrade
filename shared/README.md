Shared assets placeholder.

Use this folder to store images, fonts, icons, or design assets shared between `frontend/` and `mobile/`.

I can add a small pixel font or sample icons here if you want — tell me which assets to include.